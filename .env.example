# Intentionally empty: the Stage 5 adapter does not support environment-based
# configuration or credential injection.
#
# Do not add an endpoint, API key, token, model, or other provider secret here.
# The repository contains no HTTP transport. Offline tests explicitly inject a
# fake transport, fake model token counter, and synthetic pricing table in code.
